apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "test-http-app.fullname" . }}
  labels:
    {{- include "test-http-app.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "test-http-app.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "test-http-app.labels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}