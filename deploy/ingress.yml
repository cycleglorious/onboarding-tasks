apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-app
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: simple-app
                port:
                  number: 80
